# Backend API base URL, when the API is hosted separately from the Vite dev server.
# Leave empty to use the Vite dev proxy (default: http://localhost:8000/api).
# Example: VITE_API_BASE_URL=http://localhost:8000
VITE_API_BASE_URL=